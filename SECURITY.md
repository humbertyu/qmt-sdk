# Security

The v1 file protocol is intended for one trusted Windows machine. Restrict filesystem
permissions on the bridge root and do not place it on an untrusted network share. Avoid
including account credentials in request parameters or logs. Report vulnerabilities
privately to the repository maintainer before public disclosure.
